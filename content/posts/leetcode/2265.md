---
title: 2265
subtitle:
date: 2026-09-10T09:21:11+08:00
slug: 3cf8f2c
draft: false
author:
  name:
  link:
  email:
  avatar:
description:
keywords:
license:
weight: 0
tags:
  -
categories:
  - leetcode
hiddenFromHomePage: false
hiddenFromSearch: false
hiddenFromRelated: false
hiddenFromFeed: false
summary:
resources:
  - name: featured-image
    src: featured-image.jpg
  - name: featured-image-preview
    src: featured-image-preview.jpg
toc: true
math: false
lightgallery: false
password:
message:
repost:
  enable: false
  url:

# See details front matter: https://fixit.lruihao.cn/documentation/content-management/introduction/#front-matter
---

```java
    class Solution {
        public int averageOfSubtree(TreeNode root) {
            return dfs(root)[2];
        }

        // 返回三个值：子树和，子树节点数，满足条件的节点数
        private int[] dfs(TreeNode node) {
            if (node == null) {
                return new int[]{0, 0, 0};
            }
            int[] left = dfs(node.left);
            int[] right = dfs(node.right);
            int sum = left[0] + right[0] + node.val;
            int count = left[1] + right[1] + 1;
            return new int[]{sum, count, left[2] + right[2] + (sum / count == node.val ? 1 : 0)};
        }
    }
```

使用record
```java
    class Solution {
        public int averageOfSubtree(TreeNode root) {
            return dfs(root).ans;
        }

        private Data dfs(TreeNode node) {
            if (node == null) {
                return ZERO;
            }
            var left = dfs(node.left);
            var right = dfs(node.right);
            int sum = left.sum + right.sum + node.val;
            int count = left.count + right.count + 1;
            return new Data(sum, count, left.ans + right.ans + (sum / count == node.val ? 1 : 0));
        }
        
        private static final Data ZERO = new Data(0, 0, 0);
        
        // 子树和，子树节点数，满足条件的节点数
        private record Data(int sum, int count, int ans) {
            
        }
    }

```