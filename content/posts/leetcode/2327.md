---
title: 2327
subtitle:
date: 2025-09-09T15:19:12+08:00
slug: 3bd9b27
draft: false
author:
  name:
  link:
  email:
  avatar:
description:
keywords:
license:
weight: 0
tags:
  -
categories:
  - leetcode
hiddenFromHomePage: false
hiddenFromSearch: false
hiddenFromRelated: false
hiddenFromFeed: false
summary:
resources:
  - name: featured-image
    src: featured-image.jpg
  - name: featured-image-preview
    src: featured-image-preview.jpg
toc: true
math: false
lightgallery: false
password:
message:
repost:
  enable: false
  url:

# See details front matter: https://fixit.lruihao.cn/documentation/content-management/introduction/#front-matter
---



https://leetcode.cn/problems/number-of-people-aware-of-a-secret/description/

官解

```go
const MOD = 1000000007

type Pair struct {
	day   int
	count int
}

func peopleAwareOfSecret(n int, delay int, forget int) int {
	// 知道的队列
	know := make([]Pair, 0)
	// 分享的队列
	share := make([]Pair, 0)
	know = append(know, Pair{1, 1})
	knowCnt, shareCnt := 1, 0

	for i := 2; i <= n; i++ {
		//  队列第一个知道密码的人可以开始分享
		if len(know) > 0 && know[0].day == i-delay {
			first := know[0]
			know = know[1:]
			// knowCnt因为求余, 可能小于first.count, 因此需要加上MOD
			knowCnt = (knowCnt - first.count + MOD) % MOD
			shareCnt = (shareCnt + first.count) % MOD
			share = append(share, first)
		}
		//  队列第一个分享的人忘记
		if len(share) > 0 && share[0].day == i-forget {
			first := share[0]
			share = share[1:]
			shareCnt = (shareCnt - first.count + MOD) % MOD
		}
		// 所有分享的人都会进行一次分享,进入知道的队列
		if len(share) > 0 {
			knowCnt = (knowCnt + shareCnt) % MOD
			know = append(know, Pair{i, shareCnt})
		}
	}
	return (knowCnt + shareCnt) % MOD
}
```

